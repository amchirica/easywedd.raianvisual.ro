# Local Wrangler / OpenNext preview only — copy to .dev.vars (gitignored).
# Production secrets belong in Cloudflare Dashboard → Variables and Secrets.
#
# Required so OpenNext forDev loads `.env.local` / `.env.development*`
# instead of defaulting to the production env mode.
NEXTJS_ENV=development
