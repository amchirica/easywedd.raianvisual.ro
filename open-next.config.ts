import { defineCloudflareConfig } from "@opennextjs/cloudflare";

/**
 * OpenNext Cloudflare adapter config.
 * Incremental cache can be added later via R2 (NEXT_INC_CACHE_R2_BUCKET).
 */
export default defineCloudflareConfig();
